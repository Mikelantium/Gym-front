export interface ClassI {
    _id?: string,
    name: string,
    duration: string,
    class: number,
    type: string,
    img: string
}

export interface UserI{
    id?: string;
    email: string;
    password: string;
    role: string;
}